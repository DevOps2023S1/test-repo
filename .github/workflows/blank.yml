name: trigger jenkins job
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: trigger single Job
      uses: appleboy/jenkins-action@master
      with:
        url: "http://44.204.35.175:8080"
        user: "git-user"
        token: 11d25e075035d94b3e77ec823b6f8f7c55
        job: "build-orders-service"
